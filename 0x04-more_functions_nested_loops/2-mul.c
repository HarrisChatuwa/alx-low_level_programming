#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two numbers and returns the result
 * @a: multiplier1
 * @b: multiplier2
 * Return: result of the math
 */
int mul(int a, int b)
{
	return (a * b);
}
